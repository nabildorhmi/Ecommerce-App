---
phase: 05-wishlist-translation-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - trotinette-frontend/src/locales/en/translation.json
  - trotinette-frontend/src/locales/fr/translation.json
  - trotinette-frontend/src/features/orders/pages/AdminOrderDetailPage.tsx
  - trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx
  - trotinette-frontend/src/features/admin/components/CategoryForm.tsx
  - trotinette-frontend/src/features/admin/components/ProductForm.tsx
  - trotinette-frontend/src/features/catalog/components/ProductGallery.tsx
autonomous: true

must_haves:
  truths:
    - "Every visible UI string renders in French when FR is the active language — no English fallback text appears"
    - "Every visible UI string renders in English when EN is the active language — no raw i18n key strings appear"
    - "Switching language at any point in the app (catalog, cart, checkout, orders, profile, admin) produces fully translated text with zero untranslated strings"
    - "No hardcoded English strings remain in component JSX — all user-visible text goes through t()"
    - "Dead translation keys (deliveryZones.cityAr, smoke_test.*) are removed from both locale files"
  artifacts:
    - path: "trotinette-frontend/src/locales/en/translation.json"
      provides: "Complete EN translation file with all keys used in source"
    - path: "trotinette-frontend/src/locales/fr/translation.json"
      provides: "Complete FR translation file with all keys used in source"
  key_links:
    - from: "trotinette-frontend/src/features/orders/pages/AdminOrderDetailPage.tsx"
      to: "locales/*/translation.json"
      via: "t('orders.notFound') replacing hardcoded string"
      pattern: "t\\('orders\\.notFound'\\)"
    - from: "trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx"
      to: "locales/*/translation.json"
      via: "t() calls replacing hardcoded title attributes"
      pattern: "t\\('(common|admin)\\."
    - from: "trotinette-frontend/src/features/admin/components/CategoryForm.tsx"
      to: "locales/*/translation.json"
      via: "t() call replacing hardcoded Slug label"
      pattern: "t\\('admin\\.categories\\.slug'\\)"
---

<objective>
Audit and complete all FR/EN translations across the entire application — fix hardcoded strings, add missing keys, remove dead keys.

Purpose: Ensures I18N-02 requirement is met — every user-visible string is translated in both French and English with no raw key strings or English-only fallbacks visible.
Output: Complete, clean locale JSON files and all components using t() for every visible string.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-wishlist-translation-completion/05-RESEARCH.md
@trotinette-frontend/src/locales/en/translation.json
@trotinette-frontend/src/locales/fr/translation.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hardcoded strings in components — replace with t() calls</name>
  <files>
    trotinette-frontend/src/features/orders/pages/AdminOrderDetailPage.tsx
    trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx
    trotinette-frontend/src/features/admin/components/CategoryForm.tsx
    trotinette-frontend/src/features/admin/components/ProductForm.tsx
    trotinette-frontend/src/features/catalog/components/ProductGallery.tsx
  </files>
  <action>
    **Research identified these hardcoded strings. Fix each one:**

    1. **`AdminOrderDetailPage.tsx` line ~99:** Replace hardcoded `"Order not found"` with `t('orders.notFound')`. Ensure `useTranslation()` is imported and called in the component.

    2. **`AdminDeliveryZonesPage.tsx` lines ~352, ~360:** Replace hardcoded `title="Edit"` and `title="Delete"` on IconButton elements with `title={t('common.edit')}` and `title={t('common.delete')}`. If `common.edit` / `common.delete` keys don't exist yet, add them (check existing keys first — `admin.products.edit` / `admin.products.delete` may already exist and could be reused, OR add `common.edit` / `common.delete` for shared use).

    3. **`CategoryForm.tsx` line ~126:** Replace hardcoded `label="Slug"` with `label={t('admin.categories.slug')}`. Verify the key exists in both JSON files — if not, add it.

    4. **`ProductForm.tsx` line ~226:** Replace hardcoded `label="Stock"` with `label={t('admin.products.stock')}`. Verify the key exists in both JSON files — if not, add it.

    5. **`ProductGallery.tsx` line ~44:** Replace hardcoded `alt="Product"` with `alt={t('product.imageAlt')}`. This component needs `useTranslation()` imported if not already present.

    **After fixing the components, run a comprehensive grep** for any remaining hardcoded strings in `.tsx` files that should be translated. Search patterns:
    - `title="[A-Z]` (hardcoded title attributes)
    - `label="[A-Z]` (hardcoded label attributes)
    - `alt="[A-Z]` (hardcoded alt attributes)
    - `>"[A-Z][a-z]+` (hardcoded text content — be selective, skip code/imports)

    Fix any additional hardcoded strings found during the grep.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `grep -rn 'Order not found' src/` returns no results
    - `grep -rn 'title="Edit"' src/` returns no results in the delivery zones page
    - `grep -rn 'title="Delete"' src/` returns no results in the delivery zones page
    - `grep -rn 'label="Slug"' src/` returns no results in CategoryForm
    - `grep -rn 'label="Stock"' src/` returns no results in ProductForm
    - `grep -rn 'alt="Product"' src/` returns no results in ProductGallery
  </verify>
  <done>
    All known hardcoded English strings in components are replaced with t() calls. No user-visible text bypasses the i18n system.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit translation JSON files — add missing keys, remove dead keys</name>
  <files>
    trotinette-frontend/src/locales/en/translation.json
    trotinette-frontend/src/locales/fr/translation.json
  </files>
  <action>
    **Step 1: Extract all t() keys from source code.**

    Run a grep to extract every `t('...')` pattern from all `.tsx` and `.ts` files in the `src/` directory. Collect the unique key list.

    **Step 2: Flatten both JSON files into dot-notation key lists.**

    Read both `en/translation.json` and `fr/translation.json`. Build a complete key inventory for each.

    **Step 3: Find missing keys (in source but not in JSON).**

    Compare the extracted t() keys against both JSON key lists. For each missing key:
    - Add the EN translation (derive from the key name or existing context)
    - Add the FR translation (translate the EN string to French)

    **Step 4: Find dead keys (in JSON but not in source) and remove them.**

    Known dead keys to remove from BOTH files:
    - `deliveryZones.cityAr` — Arabic was removed in Phase 1; verify this key is not referenced in any t() call before deleting
    - `smoke_test.*` namespace — dev scaffolding from Phase 1; not used in any component

    For any other dead keys found: verify they are truly unused (not referenced dynamically or via variable interpolation), then remove.

    **Step 5: Verify FR translations are real French (not English copies).**

    Scan the FR file for entries where the FR value is identical to the EN value. These may be untranslated copies. For each identical pair:
    - If the string is a proper noun or technical term (e.g., "SKU", "MAD") — leave as-is
    - If the string is translatable English — provide the correct French translation

    **Step 6: Add any new keys needed from Task 1 fixes:**

    Ensure these keys exist in BOTH files:
    - `orders.notFound` — EN: "Order not found" / FR: "Commande introuvable"
    - `common.edit` — EN: "Edit" / FR: "Modifier" (or reuse existing admin key if appropriate)
    - `common.delete` — EN: "Delete" / FR: "Supprimer" (or reuse existing admin key if appropriate)
    - `admin.categories.slug` — EN: "Slug" / FR: "Slug" (technical term, same in both)
    - `admin.products.stock` — EN: "Stock" / FR: "Stock" (same in both languages)
    - `product.imageAlt` — EN: "Product image" / FR: "Image du produit"

    NOTE: Some of these keys may already exist (research indicated `admin.categories.slug` and `admin.products.stock` might already be in the JSON). Check before adding duplicates.
  </action>
  <verify>
    - Extract all t() keys from source → every key has an entry in BOTH en/translation.json and fr/translation.json
    - `grep -r "cityAr" src/locales/` returns no results (dead key removed)
    - `grep -r "smoke_test" src/locales/` returns no results (dead keys removed)
    - No FR translation is an untranslated English copy (except proper nouns/technical terms)
    - Both JSON files parse without errors: `node -e "require('./src/locales/en/translation.json')"` succeeds
    - Both JSON files have the same top-level key structure (every namespace in EN exists in FR and vice versa)
  </verify>
  <done>
    Both locale JSON files are complete — every t() call in source has a corresponding key in both EN and FR files. Dead keys removed. All FR values are genuine French translations. No missing translation key warnings at runtime.
  </done>
</task>

</tasks>

<verification>
1. Full app walkthrough in FR: navigate every page (catalog, product detail, login, profile, checkout, orders, admin products, admin orders, admin delivery zones, admin users) — zero English strings visible
2. Full app walkthrough in EN: same route tour — zero raw key strings visible (e.g., no "orders.notFound" text)
3. Language switch mid-flow: start in FR on catalog, switch to EN on product detail, switch back to FR on checkout — all strings translate immediately
4. JSON structure: both files have identical key trees (same namespaces, same nested structure)
5. Dead key verification: no `cityAr` or `smoke_test` references remain in locale files
</verification>

<success_criteria>
- Zero hardcoded English strings in component JSX (all user-visible text uses t())
- Every t() key in source code has entries in both EN and FR locale files
- Dead keys (deliveryZones.cityAr, smoke_test.*) removed from both files
- FR translations are genuine French (not English copies)
- Both JSON files valid and parseable
- Language switching produces complete translations on every page
</success_criteria>

<output>
After completion, create `.planning/phases/05-wishlist-translation-completion/05-02-SUMMARY.md`
</output>
