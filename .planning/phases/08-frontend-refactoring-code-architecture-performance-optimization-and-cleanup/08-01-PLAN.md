---
phase: 08-frontend-refactoring-code-architecture-performance-optimization-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trotinette-frontend/src/index.css
  - trotinette-frontend/src/animations.css
  - trotinette-frontend/src/features/catalog/pages/ProductDetailPage.tsx
  - trotinette-frontend/src/features/checkout/pages/CheckoutPage.tsx
  - trotinette-frontend/src/App.tsx
  - trotinette-frontend/src/App.css
  - trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx
autonomous: true

must_haves:
  truths:
    - "No dead files (App.tsx, App.css, AdminDeliveryZonesPage.tsx) exist in the source tree"
    - "CSS keyframe animations are defined in exactly one location with no duplicates"
    - "TypeScript compilation succeeds with zero errors (npx tsc --noEmit)"
    - "No untyped `any` annotations exist in CheckoutPage.tsx error handlers"
  artifacts:
    - path: "trotinette-frontend/src/index.css"
      provides: "Consolidated CSS with all keyframes, no duplicates"
    - path: "trotinette-frontend/src/animations.css"
      provides: "Animation keyframes as single source of truth (no overlap with index.css)"
    - path: "trotinette-frontend/src/features/catalog/pages/ProductDetailPage.tsx"
      provides: "Fixed type cast with promo_price and is_on_sale properties"
  key_links:
    - from: "trotinette-frontend/src/main.tsx"
      to: "trotinette-frontend/src/index.css"
      via: "CSS import"
      pattern: "import.*index\\.css"
---

<objective>
Remove dead code, fix TypeScript errors, consolidate duplicate CSS, and eliminate `any` types — cleaning up technical debt before the architecture changes in plans 02 and 03.

Purpose: A clean codebase is prerequisite for safe refactoring. Dead files confuse developers, duplicate CSS causes specificity bugs, TypeScript errors block production builds, and `any` types defeat type safety.
Output: Zero dead files, zero duplicate keyframes, zero TypeScript errors, zero `any` annotations in modified files.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@trotinette-frontend/src/index.css
@trotinette-frontend/src/animations.css
@trotinette-frontend/src/features/catalog/pages/ProductDetailPage.tsx
@trotinette-frontend/src/features/checkout/pages/CheckoutPage.tsx
@trotinette-frontend/src/main.tsx
@trotinette-frontend/src/app/router.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead files and fix TypeScript errors</name>
  <files>
    trotinette-frontend/src/App.tsx
    trotinette-frontend/src/App.css
    trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx
    trotinette-frontend/src/features/catalog/pages/ProductDetailPage.tsx
    trotinette-frontend/src/features/checkout/pages/CheckoutPage.tsx
  </files>
  <action>
1. **Delete dead files** — these are not imported anywhere:
   - `src/App.tsx` (Vite boilerplate, never imported — main.tsx uses router directly)
   - `src/App.css` (imported only by App.tsx which is dead)
   - `src/features/admin/pages/AdminDeliveryZonesPage.tsx` (feature removed, not in router.tsx)
   Verify before deletion: grep the entire src/ tree for imports of each file to confirm they are truly unreferenced.

2. **Fix TypeScript error in ProductDetailPage.tsx** around line 156:
   The type cast `as ProductVariantDisplay` is missing `promo_price` and `is_on_sale` properties. Add them:
   ```typescript
   {
     id: product.default_variant.id,
     sku: product.default_variant.sku,
     price: product.default_variant.price,
     promo_price: product.default_variant.promo_price,  // ADD
     is_on_sale: product.default_variant.is_on_sale,    // ADD
     stock: product.default_variant.stock,
     attribute_values: [],
   } as ProductVariantDisplay
   ```
   Check the `ProductVariantDisplay` type definition first to see exactly which fields are required.

3. **Replace `any` types in CheckoutPage.tsx** (lines ~88 and ~102):
   - `onError: (error: any)` should be typed. Use the Axios error type pattern already established in the codebase:
   ```typescript
   import type { AxiosError } from 'axios';
   // ...
   onError: (error: AxiosError<{ message?: string }>) => {
   ```
   Or use a more specific API error type if one exists in the project. Check how other mutation error handlers type their errors.
  </action>
  <verify>
    Run `npx tsc --noEmit` from trotinette-frontend/ — must exit 0 with no errors.
    Run `ls src/App.tsx src/App.css src/features/admin/pages/AdminDeliveryZonesPage.tsx` — all three must return "No such file".
    Run `grep -r "any" src/features/checkout/pages/CheckoutPage.tsx` — should show no `: any` type annotations.
  </verify>
  <done>
    App.tsx, App.css, and AdminDeliveryZonesPage.tsx are deleted. ProductDetailPage.tsx compiles without type errors. CheckoutPage.tsx has zero `any` annotations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Consolidate duplicate CSS keyframes</name>
  <files>
    trotinette-frontend/src/index.css
    trotinette-frontend/src/animations.css
  </files>
  <action>
1. **Identify duplicates:** The following keyframes exist in BOTH index.css and animations.css:
   - `shimmer`
   - `gradient-shift`
   - `pulse-dot`
   - `border-glow`

2. **Strategy:** Keep animations.css as the single source of truth for all keyframe definitions. Remove the duplicate keyframes from index.css. The keyframes unique to index.css (`miraiSlideUp`, `scanlines`) should be moved to animations.css.

3. **In index.css:**
   - Remove `@keyframes shimmer` (lines ~150-159)
   - Remove `@keyframes gradient-shift` (lines ~135-149)
   - Remove `@keyframes pulse-dot` (lines ~161-175)
   - Remove `@keyframes border-glow` (lines ~176-184)
   - Move `@keyframes miraiSlideUp` to animations.css, then remove from index.css
   - Move `@keyframes scanlines` to animations.css, then remove from index.css
   - Keep all non-keyframe rules (CSS custom properties, utility classes, global resets) in index.css

4. **In animations.css:**
   - Add `@keyframes miraiSlideUp` (from index.css)
   - Add `@keyframes scanlines` (from index.css)
   - Result: animations.css is the single source for ALL keyframe definitions

5. **Verify imports:** Ensure both files are imported in main.tsx (or wherever CSS is loaded). Both must be imported for styles to work.

6. **Also remove the `@keyframes logo-spin` from App.css** — App.css is already deleted in Task 1, so this is handled.
  </action>
  <verify>
    Run `grep -c "@keyframes" src/index.css` — should be 0 (no keyframes left in index.css).
    Run `grep "@keyframes" src/animations.css | sort` — should list all keyframes with no duplicates.
    Run `npm run build` from trotinette-frontend/ — build must succeed.
  </verify>
  <done>
    All @keyframes definitions exist in animations.css only. index.css contains zero keyframe definitions. No duplicate keyframe names across any CSS file. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` exits 0 — no TypeScript compilation errors
2. `npm run build` succeeds — production build works
3. No dead files remain (App.tsx, App.css, AdminDeliveryZonesPage.tsx deleted)
4. `grep -r "@keyframes" src/index.css` returns empty — no keyframes in index.css
5. No duplicate keyframe names across CSS files
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- Production build succeeds
- Three dead files removed from source tree
- CSS keyframes consolidated into single file with zero duplication
- Zero `any` type annotations in CheckoutPage.tsx
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-refactoring-code-architecture-performance-optimization-and-cleanup/08-01-SUMMARY.md`
</output>
