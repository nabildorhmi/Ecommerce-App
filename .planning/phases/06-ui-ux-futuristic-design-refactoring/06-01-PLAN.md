---
phase: 06-ui-ux-futuristic-design-refactoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trotinette-frontend/package.json
  - trotinette-frontend/vite.config.ts
  - trotinette-frontend/src/main.tsx
  - trotinette-frontend/src/shared/animations/variants.ts
  - trotinette-frontend/src/shared/animations/transitions.ts
  - trotinette-frontend/src/shared/hooks/useReducedMotion.ts
  - trotinette-frontend/src/shared/hooks/use3DSupport.ts
autonomous: true

must_haves:
  truths:
    - "Framer Motion, React Three Fiber, react-countup and all peer deps are installed and importable"
    - "Vite builds successfully with Three.js in a separate chunk for code-splitting"
    - "MotionConfig wrapper respects OS-level reduced motion preference globally"
    - "Shared animation presets (modal, drawer, button, fadeIn, stagger) are importable from a single file"
  artifacts:
    - path: "trotinette-frontend/src/shared/animations/variants.ts"
      provides: "Framer Motion variant presets for modal, drawer, button, fadeIn, staggerChildren"
      exports: ["modalVariants", "drawerVariants", "buttonVariants", "fadeInUp", "staggerContainer"]
    - path: "trotinette-frontend/src/shared/animations/transitions.ts"
      provides: "Easing curves, duration constants, spring configs"
      exports: ["springSnappy", "springGentle", "easeCubic", "durations"]
    - path: "trotinette-frontend/src/shared/hooks/useReducedMotion.ts"
      provides: "Re-exported Framer Motion useReducedMotion hook with app defaults"
      exports: ["useReducedMotion"]
    - path: "trotinette-frontend/src/shared/hooks/use3DSupport.ts"
      provides: "WebGL detection hook for 3D fallback logic"
      exports: ["use3DSupport"]
  key_links:
    - from: "trotinette-frontend/src/main.tsx"
      to: "framer-motion MotionConfig"
      via: "MotionConfig wrapper around RouterProvider"
      pattern: "MotionConfig"
---

<objective>
Install animation and 3D dependencies, configure Vite for Three.js tree-shaking, create shared animation presets and hooks, and wrap the app in MotionConfig for global reduced-motion support.

Purpose: Every subsequent plan depends on these shared utilities and installed packages.
Output: Working animation infrastructure ready for all UI refactoring plans.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-RESEARCH.md
@trotinette-frontend/package.json
@trotinette-frontend/vite.config.ts
@trotinette-frontend/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and configure Vite for 3D/animation bundle splitting</name>
  <files>
    trotinette-frontend/package.json
    trotinette-frontend/vite.config.ts
  </files>
  <action>
    1. From trotinette-frontend/, run:
       npm install three @react-three/fiber @react-three/drei framer-motion framer-motion-3d react-countup

    2. Update vite.config.ts to add manual chunks for Three.js and animation libraries:
       - Add rollupOptions.output.manualChunks splitting 'three', '@react-three/fiber', '@react-three/drei' into 'three-vendor'
       - Add 'framer-motion', 'framer-motion-3d' into 'animation-vendor'
       - Add optimizeDeps.include for ['three', '@react-three/fiber', '@react-three/drei'] to pre-bundle in dev
       - Set build.target to 'es2020' for modern optimizations
       - Keep existing plugins and test config unchanged

    3. Verify: Run `npm run build` — must succeed with no errors and produce separate chunk files for three-vendor and animation-vendor.
  </action>
  <verify>
    cd trotinette-frontend && npm run build
    # Check dist/assets/ for separate three-vendor and animation-vendor chunks
  </verify>
  <done>Build succeeds, Three.js and Framer Motion are in separate vendor chunks, dev server starts without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create shared animation presets, hooks, and MotionConfig wrapper</name>
  <files>
    trotinette-frontend/src/shared/animations/variants.ts
    trotinette-frontend/src/shared/animations/transitions.ts
    trotinette-frontend/src/shared/hooks/useReducedMotion.ts
    trotinette-frontend/src/shared/hooks/use3DSupport.ts
    trotinette-frontend/src/main.tsx
  </files>
  <action>
    1. Create src/shared/animations/variants.ts with Framer Motion Variants objects:
       - modalVariants: hidden (opacity:0, scale:0.85, filter:'blur(8px)'), visible (opacity:1, scale:1, filter:'blur(0px)', spring damping:25 stiffness:300), exit (opacity:0, scale:0.9, filter:'blur(4px)', duration:0.2)
       - drawerVariants: hidden (x:'100%'), visible (x:0, spring damping:30 stiffness:300), exit (x:'100%', duration:0.25)
       - fadeInUp: hidden (opacity:0, y:24), visible (opacity:1, y:0, duration:0.5)
       - staggerContainer: hidden {}, visible (staggerChildren:0.08)
       - buttonVariants: hover (scale:1.04, y:-2), tap (scale:0.96)
       - tableRowVariants: hidden (opacity:0, x:-16), visible with custom delay (i) => ({ opacity:1, x:0, transition: { delay: i * 0.04, duration:0.3 } })
       NOTE: For color-related animations, use type:'tween' (not spring) to avoid bounce overshoot per research pitfall #6.

    2. Create src/shared/animations/transitions.ts:
       - springSnappy: { type:'spring', damping:20, stiffness:300 }
       - springGentle: { type:'spring', damping:30, stiffness:200 }
       - easeCubic: { type:'tween', ease:[0.25, 0.46, 0.45, 0.94], duration:0.4 }
       - durations: { fast:0.15, normal:0.3, slow:0.5, entrance:0.6 }

    3. Create src/shared/hooks/useReducedMotion.ts:
       - Re-export useReducedMotion from 'framer-motion'
       - Export a helper: getMotionProps(shouldReduce: boolean) that returns {} if reduced, or standard animate props if not

    4. Create src/shared/hooks/use3DSupport.ts:
       - Check for WebGL support via canvas.getContext('webgl2') || canvas.getContext('webgl')
       - Return { supportsWebGL: boolean, supportsWebGL2: boolean }
       - Use useMemo to avoid re-checking on every render

    5. Update src/main.tsx:
       - Import { MotionConfig, useReducedMotion } from 'framer-motion'
       - Create a small MotionConfigWrapper component that calls useReducedMotion() and wraps children in <MotionConfig reducedMotion={shouldReduce ? 'always' : 'never'}>
       - Wrap the existing RTLProvider + RouterProvider tree inside MotionConfigWrapper
       - Keep all other imports (QueryClientProvider, ReactQueryDevtools, etc.) unchanged
  </action>
  <verify>
    cd trotinette-frontend && npx tsc --noEmit
    # Verify no TypeScript errors and all new files compile
  </verify>
  <done>All shared animation files exist with correct exports, main.tsx wraps app in MotionConfig, TypeScript compiles with no errors, dev server runs normally</done>
</task>

</tasks>

<verification>
- `npm run build` in trotinette-frontend succeeds
- `npx tsc --noEmit` passes with no errors
- dist/assets/ contains separate vendor chunks for three and animation libraries
- Importing from 'framer-motion', 'three', '@react-three/fiber', 'react-countup' works
- App renders normally in dev (no visual changes yet, just infrastructure)
</verification>

<success_criteria>
All animation and 3D dependencies installed, Vite configured for optimal bundle splitting, shared animation presets importable, MotionConfig wrapper active with reduced-motion support, app builds and runs without regression.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-futuristic-design-refactoring/06-01-SUMMARY.md`
</output>
