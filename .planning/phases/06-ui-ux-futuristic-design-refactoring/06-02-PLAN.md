---
phase: 06-ui-ux-futuristic-design-refactoring
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - trotinette-frontend/src/features/home/components/Hero3D.tsx
  - trotinette-frontend/src/features/home/components/ParticleField.tsx
  - trotinette-frontend/src/features/home/components/Hero3DFallback.tsx
  - trotinette-frontend/src/features/home/pages/HomePage.tsx
autonomous: true

must_haves:
  truths:
    - "Homepage hero section renders an interactive 3D scene with floating particles that react to cursor movement"
    - "Hero section has smooth entrance animations for text and CTA button"
    - "CTA button glows with animated shadow"
    - "Devices without WebGL see a graceful 2D CSS-animated fallback instead of a blank or broken scene"
    - "Featured product sections and promo banners animate in on scroll via Framer Motion"
  artifacts:
    - path: "trotinette-frontend/src/features/home/components/Hero3D.tsx"
      provides: "R3F Canvas wrapper with lazy loading, Suspense fallback, and 2D fallback for non-WebGL devices"
      min_lines: 40
    - path: "trotinette-frontend/src/features/home/components/ParticleField.tsx"
      provides: "InstancedMesh particle system with cursor-reactive floating shapes"
      min_lines: 60
    - path: "trotinette-frontend/src/features/home/components/Hero3DFallback.tsx"
      provides: "CSS-animated 2D fallback hero replacing concentric rings for non-WebGL devices"
      min_lines: 20
    - path: "trotinette-frontend/src/features/home/pages/HomePage.tsx"
      provides: "Refactored homepage using Hero3D, Framer Motion entrance animations on all sections"
  key_links:
    - from: "trotinette-frontend/src/features/home/pages/HomePage.tsx"
      to: "Hero3D.tsx"
      via: "React.lazy() dynamic import for code-splitting the 3D bundle"
      pattern: "lazy.*Hero3D"
    - from: "trotinette-frontend/src/features/home/components/ParticleField.tsx"
      to: "@react-three/fiber useFrame + useThree"
      via: "useFrame render loop for particle animation, useThree for pointer/viewport"
      pattern: "useFrame|useThree"
---

<objective>
Replace the homepage hero with an interactive 3D particle scene using React Three Fiber, add Framer Motion entrance animations to all homepage sections, and provide a 2D fallback for devices without WebGL.

Purpose: Success criteria #1 — interactive 3D hero with floating shapes reacting to cursor, smooth entrance animations, glowing CTA.
Output: Fully animated homepage with 3D hero scene.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-RESEARCH.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-01-SUMMARY.md
@trotinette-frontend/src/features/home/pages/HomePage.tsx
@trotinette-frontend/src/shared/animations/variants.ts
@trotinette-frontend/src/shared/animations/transitions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3D hero components (ParticleField, Hero3D, Hero3DFallback)</name>
  <files>
    trotinette-frontend/src/features/home/components/ParticleField.tsx
    trotinette-frontend/src/features/home/components/Hero3D.tsx
    trotinette-frontend/src/features/home/components/Hero3DFallback.tsx
  </files>
  <action>
    1. Create ParticleField.tsx — an InstancedMesh-based particle system:
       - Use instancedMesh with ~50-80 icosahedron particles (keep count low for 60fps)
       - Create module-level reusable temp objects (tempMatrix, tempPosition, tempQuaternion, tempScale) to avoid GC thrashing — CRITICAL per research
       - In useFrame: read pointer from useThree(), animate each particle with sin/cos floating motion + cursor influence (repel/attract based on distance)
       - Material: MeshStandardMaterial with MIRAI_CYAN (#00C2FF), emissive #00C2FF at intensity 0.4
       - Use useMemo for initial particle positions (random spread in a sphere/box shape)
       - Do NOT allocate new Vector3 inside useFrame — reuse temp objects only

    2. Create Hero3D.tsx — the R3F Canvas wrapper:
       - Import use3DSupport hook from shared/hooks
       - If !supportsWebGL, render Hero3DFallback instead
       - Canvas with frameloop="always", dpr={[1, 1.5]} (cap at 1.5 for performance)
       - PerspectiveCamera at [0, 0, 5]
       - ambientLight intensity 0.3 + pointLight at [10, 10, 10] with color #00C2FF intensity 0.8
       - Render ParticleField inside Canvas
       - Wrap the entire hero in a Box with height 520-600px, position relative
       - Overlay: absolute-positioned Framer Motion div with hero text content (brand tagline, headline "L'AVENIR DE LA MOBILITE URBAINE", subtitle, CTA buttons)
       - CTA "Acheter maintenant" uses existing pulse-glow animation from animations.css + Link to /products
       - Entrance animation: text fades in from below with staggered delay (title 0.3s, subtitle 0.5s, buttons 0.7s, stats 0.9s) using framer-motion
       - Stats bar at bottom (same 3 stats as current: 500+ clients, 48h delivery, 2 ans garantie)

    3. Create Hero3DFallback.tsx:
       - A simplified 2D hero matching the current concentric-rings aesthetic (keep existing CSS keyframes)
       - Floating gradient orbs with float animation
       - Concentric rings with rotate-slow animation
       - Same text overlay as Hero3D (share layout, just different background)
       - This is shown ONLY when WebGL is not available
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Three components exist, compile without errors, ParticleField uses InstancedMesh with no allocations in useFrame, Hero3D code-splits correctly</done>
</task>

<task type="auto">
  <name>Task 2: Refactor HomePage to use Hero3D and add Framer Motion entrance animations</name>
  <files>
    trotinette-frontend/src/features/home/pages/HomePage.tsx
  </files>
  <action>
    1. Replace the HeroBanner function with a lazy-loaded Hero3D:
       - const Hero3D = lazy(() => import('../components/Hero3D'))
       - Wrap in Suspense with a skeleton/loading fallback (simple Box with gradient background matching hero colors)
       - Remove the old HeroBanner function entirely

    2. Add Framer Motion entrance animations to remaining sections:
       - CategoriesStrip: wrap in motion.div with fadeInUp variant, animate on whileInView with viewport={{ once: true, amount: 0.3 }}
       - FeaturedSection: wrap section container in motion.div with fadeInUp, each CategoryFeaturedRow gets stagger animation
       - CategoryFeaturedRow product cards: animate with staggered fadeInUp using staggerContainer + fadeInUp from shared variants
       - PromoBanners: each banner card wraps in motion.div with fadeInUp + whileHover={{ y: -8, boxShadow: '...' }} — replace existing CSS hover with Framer Motion for smoother animation

    3. Import motion from 'framer-motion' and variants from shared/animations/variants.

    4. Keep all existing data fetching (useFeaturedProducts, useCategories), product card links, and formatting logic UNCHANGED. Only modify the animation/presentation layer.

    5. Ensure the existing scroll-based horizontal product row still works (scrollRef + scroll buttons). Do NOT break the horizontal scroll behavior.
  </action>
  <verify>
    cd trotinette-frontend && npx tsc --noEmit && npm run build
    # Verify build succeeds and Hero3D appears as separate lazy chunk
  </verify>
  <done>HomePage renders with 3D hero (or 2D fallback), all sections have entrance animations, horizontal scroll works, build produces separate 3D chunk via lazy loading</done>
</task>

</tasks>

<verification>
- Homepage loads with 3D particle scene in hero area
- Moving cursor causes particles to react (subtle movement toward/away from cursor)
- Hero text and CTA fade in with staggered animation
- Scrolling down triggers entrance animations on categories, featured products, and promo banners
- CTA button links to /products and has glowing pulse animation
- Build produces a separate lazy chunk for the 3D hero
- No TypeScript errors
</verification>

<success_criteria>
Interactive 3D hero with cursor-reactive particles renders on the homepage, all sections animate on entrance, CTA button glows, 2D fallback exists for non-WebGL devices, build succeeds with code-split 3D bundle.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-futuristic-design-refactoring/06-02-SUMMARY.md`
</output>
