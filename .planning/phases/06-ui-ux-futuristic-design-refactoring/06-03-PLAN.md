---
phase: 06-ui-ux-futuristic-design-refactoring
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - trotinette-frontend/src/features/auth/pages/LoginPage.tsx
  - trotinette-frontend/src/features/auth/pages/ForgotPasswordPage.tsx
  - trotinette-frontend/src/features/auth/pages/ResetPasswordPage.tsx
  - trotinette-frontend/src/features/auth/components/LoginForm.tsx
  - trotinette-frontend/src/features/auth/components/RegisterForm.tsx
autonomous: true

must_haves:
  truths:
    - "Login and register pages display a centered glassmorphic card with backdrop-filter blur"
    - "A soft background animation (floating gradient orbs) plays behind the auth card"
    - "Input fields have animated focus states — border glow expands and label floats with smooth transition"
    - "Tab switch between login and register animates with crossfade or slide"
    - "Form submission shows a loading spinner transition and success feedback before redirect"
  artifacts:
    - path: "trotinette-frontend/src/features/auth/pages/LoginPage.tsx"
      provides: "Refactored auth page with glassmorphic card, background animation, animated tab transitions"
      min_lines: 60
    - path: "trotinette-frontend/src/features/auth/components/LoginForm.tsx"
      provides: "Login form with animated input focus states and loading transition"
    - path: "trotinette-frontend/src/features/auth/components/RegisterForm.tsx"
      provides: "Register form with animated input focus states and loading transition"
  key_links:
    - from: "trotinette-frontend/src/features/auth/pages/LoginPage.tsx"
      to: "framer-motion AnimatePresence"
      via: "AnimatePresence for tab switch animation between login and register"
      pattern: "AnimatePresence"
    - from: "trotinette-frontend/src/features/auth/components/LoginForm.tsx"
      to: "framer-motion motion"
      via: "motion.div for form entrance and button loading state"
      pattern: "motion\\.div|motion\\.button"
---

<objective>
Refactor login, register, forgot-password, and reset-password pages to use centered glassmorphic cards with floating background animations, animated input focus states, smooth tab transitions, and loading/success transitions on form submission.

Purpose: Success criteria #2 — auth pages with glassmorphic cards, soft background animations, floating labels, animated focus states, and loading transitions.
Output: Premium-feeling auth flow with smooth animations throughout.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-RESEARCH.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-01-SUMMARY.md
@trotinette-frontend/src/features/auth/pages/LoginPage.tsx
@trotinette-frontend/src/features/auth/components/LoginForm.tsx
@trotinette-frontend/src/features/auth/components/RegisterForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glassmorphic background and animated tab transitions to LoginPage</name>
  <files>
    trotinette-frontend/src/features/auth/pages/LoginPage.tsx
    trotinette-frontend/src/features/auth/pages/ForgotPasswordPage.tsx
    trotinette-frontend/src/features/auth/pages/ResetPasswordPage.tsx
  </files>
  <action>
    1. Refactor LoginPage.tsx:
       - Add a full-page background layer behind the card with:
         - Dark gradient base: linear-gradient(135deg, #0B0B0E 0%, #0d1a24 50%, #0B0B0E 100%)
         - 2-3 floating gradient orbs (radial-gradient circles) using CSS float animation from animations.css
         - Noise texture overlay (same SVG pattern as current HomePage hero)
       - Wrap the Paper card in motion.div with entrance animation: initial={{ opacity:0, y:30, scale:0.95 }}, animate={{ opacity:1, y:0, scale:1 }}, spring transition
       - Card styling: enhance existing glassmorphism — rgba(17,17,22,0.85) background (slightly more opaque for contrast), blur(20px), border rgba(255,255,255,0.08), boxShadow '0 24px 64px rgba(0,0,0,0.5)', borderRadius 20px
       - Add a subtle top-edge gradient line on the card: linear-gradient(90deg, transparent, rgba(0,194,255,0.3), transparent)
       - Use AnimatePresence mode="wait" around the tab content area. When tab changes, the current form exits (opacity:0, x:-20) and new form enters (opacity:0, x:20 -> opacity:1, x:0). Give each form a key={tab} so AnimatePresence detects the change.
       - Keep ALL existing auth logic (mutations, error handling, navigation, useAuthStore) exactly as-is. Only change presentation.

    2. Apply similar glassmorphic background to ForgotPasswordPage.tsx and ResetPasswordPage.tsx:
       - Same floating orbs background
       - Same glassmorphic card entrance animation
       - Same enhanced card styling
       - Keep existing form logic and react-hook-form handling unchanged
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Auth pages render with glassmorphic cards over animated backgrounds, tab transitions animate smoothly, all auth logic unchanged</done>
</task>

<task type="auto">
  <name>Task 2: Add animated input focus states and loading transitions to auth forms</name>
  <files>
    trotinette-frontend/src/features/auth/components/LoginForm.tsx
    trotinette-frontend/src/features/auth/components/RegisterForm.tsx
  </files>
  <action>
    1. In both LoginForm.tsx and RegisterForm.tsx:
       - Wrap the entire form in motion.div with fadeInUp animation (from shared variants)
       - Each TextField wraps in motion.div with staggered entrance: index * 0.1s delay
       - Enhanced input focus styling (supplement MUI theme, don't replace):
         - On focus: border transitions to #00C2FF with boxShadow '0 0 16px rgba(0,194,255,0.25)' and background shifts to rgba(0,194,255,0.04)
         - Label color on focus: #00C2FF
         - These may already be partly in theme.ts MuiTextField overrides — only ADD what's missing, don't duplicate
       - Submit button: wrap in motion.button (or use motion(Button)) with whileHover={{ scale:1.02 }} and whileTap={{ scale:0.98 }}
       - Loading state: when mutation isPending, button shows CircularProgress with smooth opacity transition (animate opacity from 1 to 0 on text, 0 to 1 on spinner)
       - Error alert: wrap in AnimatePresence + motion.div with fadeInUp so it slides in when error appears and slides out when cleared

    2. Keep all existing validation, react-hook-form usage, field names, and submit handlers UNCHANGED. Only add animation wrappers and enhanced focus styles.

    3. Ensure form still works: login with valid/invalid credentials, register with validation errors, all error messages display correctly.
  </action>
  <verify>
    cd trotinette-frontend && npx tsc --noEmit && npm run build
  </verify>
  <done>Auth forms have staggered entrance animations, enhanced focus glow on inputs, animated submit button with loading transition, animated error alerts, all auth functionality preserved</done>
</task>

</tasks>

<verification>
- Login page shows floating orb background with glassmorphic card
- Switching between login/register tabs animates with crossfade
- Input fields glow cyan on focus
- Submit button scales on hover/tap and shows spinner during loading
- Error messages animate in/out
- Login, register, forgot-password, reset-password all function correctly
- TypeScript compiles, build succeeds
</verification>

<success_criteria>
Auth pages use centered glassmorphic cards with soft background animations, floating labels with animated focus states, smooth tab transitions, and loading/success transitions. All auth functionality works identically to before.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-futuristic-design-refactoring/06-03-SUMMARY.md`
</output>
