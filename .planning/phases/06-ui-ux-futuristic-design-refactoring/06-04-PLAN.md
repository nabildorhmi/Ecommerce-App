---
phase: 06-ui-ux-futuristic-design-refactoring
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - trotinette-frontend/src/features/admin/components/AnimatedKPICard.tsx
  - trotinette-frontend/src/features/admin/components/AnimatedChart.tsx
  - trotinette-frontend/src/features/admin/pages/AdminDashboardPage.tsx
  - trotinette-frontend/src/features/admin/pages/AdminProductsPage.tsx
  - trotinette-frontend/src/features/admin/pages/AdminOrdersPage.tsx
  - trotinette-frontend/src/features/admin/pages/AdminUsersPage.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard KPI cards animate numbers from 0 to target value using count-up when scrolled into view"
    - "KPI cards have hover elevation with glowing shadow"
    - "Dashboard charts animate in with smooth entrance transitions"
    - "Admin tables have rounded rows with hover elevation effect and a cyan-tinted glow"
    - "Admin table headers are sticky with backdrop-filter blur when scrolling"
  artifacts:
    - path: "trotinette-frontend/src/features/admin/components/AnimatedKPICard.tsx"
      provides: "Reusable KPI card with react-countup, Framer Motion hover, glassmorphic surface"
      exports: ["AnimatedKPICard"]
      min_lines: 30
    - path: "trotinette-frontend/src/features/admin/components/AnimatedChart.tsx"
      provides: "Wrapper for Recharts with Framer Motion entrance animation"
      exports: ["AnimatedChart"]
    - path: "trotinette-frontend/src/features/admin/pages/AdminDashboardPage.tsx"
      provides: "Refactored dashboard using AnimatedKPICard and AnimatedChart"
  key_links:
    - from: "trotinette-frontend/src/features/admin/components/AnimatedKPICard.tsx"
      to: "react-countup"
      via: "CountUp component with enableScrollSpy and scrollSpyOnce"
      pattern: "CountUp.*enableScrollSpy.*scrollSpyOnce"
    - from: "trotinette-frontend/src/features/admin/pages/AdminDashboardPage.tsx"
      to: "AnimatedKPICard"
      via: "import and render for each KPI metric"
      pattern: "AnimatedKPICard"
---

<objective>
Create animated KPI cards with count-up numbers, add entrance animations to dashboard charts, and enhance all admin tables with hover elevation, rounded rows, and sticky blur headers.

Purpose: Success criteria #3 — admin tables with rounded rows, hover elevation, animated sorting, sticky blur headers; KPI cards with count-up; charts with smooth transitions.
Output: Polished admin dashboard and table experience.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-RESEARCH.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-01-SUMMARY.md
@trotinette-frontend/src/features/admin/pages/AdminDashboardPage.tsx
@trotinette-frontend/src/features/admin/pages/AdminProductsPage.tsx
@trotinette-frontend/src/shared/animations/variants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimatedKPICard and AnimatedChart components, refactor AdminDashboardPage</name>
  <files>
    trotinette-frontend/src/features/admin/components/AnimatedKPICard.tsx
    trotinette-frontend/src/features/admin/components/AnimatedChart.tsx
    trotinette-frontend/src/features/admin/pages/AdminDashboardPage.tsx
  </files>
  <action>
    1. Create AnimatedKPICard.tsx:
       - Props: { label: string, value: number, prefix?: string, suffix?: string, decimals?: number, icon?: ReactNode, color?: string }
       - Wrap in motion.div with fadeInUp entrance + whileHover: { y: -6, boxShadow: '0 16px 40px rgba(0,194,255,0.15)' }
       - Use MUI Card with glassmorphic styling: rgba(22,22,28,0.6) background, blur(16px), border rgba(255,255,255,0.06)
       - Add a subtle top-border gradient: borderTop '2px solid' with color prop or default cyan
       - Number: <CountUp end={value} prefix={prefix} suffix={suffix} decimals={decimals} duration={2} enableScrollSpy scrollSpyOnce />
       - IMPORTANT: Always include scrollSpyOnce to prevent repeated animations per research pitfall #7
       - Icon renders in a small rounded box with rgba(0,194,255,0.1) background

    2. Create AnimatedChart.tsx:
       - A simple wrapper component that wraps children (Recharts ResponsiveContainer) in motion.div
       - whileInView entrance: fadeInUp from shared variants, viewport={{ once: true, amount: 0.2 }}
       - Glassmorphic Paper wrapper matching existing dashboard chart papers
       - Props: { title: string, children: ReactNode }

    3. Refactor AdminDashboardPage.tsx:
       - Replace the 4 plain Card KPI blocks with AnimatedKPICard:
         - "Commandes totales" — icon: ShoppingCartIcon, value: data.kpis.total_orders
         - "Chiffre d'affaires" — icon: TrendingUpIcon, value: data.kpis.total_revenue, format as MAD via prefix/suffix or pass pre-formatted
         - "Panier moyen" — icon: ReceiptIcon, value: data.kpis.average_order_value
         - "Nouveaux clients" — icon: PeopleIcon, value: data.kpis.new_customers
         NOTE: revenue and average_order_value are in centimes — divide by 100 for CountUp display and add suffix " MAD"
       - Wrap KPI grid in motion.div with staggerContainer variant for staggered entrance
       - Replace each chart Paper with AnimatedChart wrapper — keep Recharts content unchanged
       - Add Recharts animationDuration={1500} and animationEasing="ease-out" to Bar, Line, Pie components for smoother chart entrance (Recharts has built-in animation support)
       - Keep ALL existing filter logic, data fetching, and status constants unchanged
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Dashboard KPIs animate count-up, charts have entrance animations, glassmorphic card styling applied, all dashboard data and filters work correctly</done>
</task>

<task type="auto">
  <name>Task 2: Enhance admin tables with hover elevation, rounded rows, and sticky blur headers</name>
  <files>
    trotinette-frontend/src/features/admin/pages/AdminProductsPage.tsx
    trotinette-frontend/src/features/admin/pages/AdminOrdersPage.tsx
    trotinette-frontend/src/features/admin/pages/AdminUsersPage.tsx
  </files>
  <action>
    1. Add enhanced table styling to AdminProductsPage, AdminOrdersPage (if it has a table), and AdminUsersPage. For each page that uses MUI Table:

       a. TableContainer: add sx overrides:
          - maxHeight for scroll (if not already set), borderRadius: '12px'
          - border: '1px solid rgba(255,255,255,0.06)'

       b. TableHead: add sticky header styling:
          - position: 'sticky', top: 0, zIndex: 10
          - backgroundColor: 'rgba(17,17,22,0.95)'
          - backdropFilter: 'blur(12px)'
          - Add a gradient bottom border via '&::after' pseudo-element: linear-gradient(90deg, transparent, rgba(0,194,255,0.3), transparent)

       c. TableRow (body rows): add hover elevation:
          - transition: 'all 0.2s ease'
          - '&:hover': { backgroundColor: 'rgba(0,194,255,0.04)', boxShadow: '0 4px 16px rgba(0,194,255,0.08)' }
          - borderRadius on td cells: first-child borderRadius '8px 0 0 8px', last-child '0 8px 8px 0'

       d. Wrap table body rows in motion.tr (if practical) with staggered fadeIn using tableRowVariants from shared variants. Use custom={index} for stagger delay.
          NOTE: If motion.tr causes issues with MUI TableRow, use CSS transitions instead (the hover effects above). Only add motion.tr if it works cleanly with MUI's component structure.

    2. Keep all existing table data, pagination, actions, and filter logic UNCHANGED. This is purely a visual enhancement pass.

    3. If a page uses a simple list/grid instead of MUI Table, skip that page (no forced table conversion).
  </action>
  <verify>
    cd trotinette-frontend && npx tsc --noEmit && npm run build
  </verify>
  <done>Admin tables have sticky blur headers, hover elevation on rows, rounded row corners, staggered entrance animation (if motion.tr works), all table functionality unchanged</done>
</task>

</tasks>

<verification>
- Dashboard loads and KPI numbers count up from 0 to actual values
- KPI cards elevate on hover with cyan glow shadow
- Charts animate in when scrolled into view
- Admin product/order/user tables have sticky headers that blur content behind
- Table rows glow slightly on hover
- All admin data, filters, pagination, and actions work correctly
- Build succeeds, no TypeScript errors
</verification>

<success_criteria>
Dashboard KPI cards show count-up animations with hover effects, charts animate on entrance, admin tables feature sticky blur headers and hover-elevation rows. All admin functionality preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-futuristic-design-refactoring/06-04-SUMMARY.md`
</output>
