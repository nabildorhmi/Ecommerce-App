---
phase: 06-ui-ux-futuristic-design-refactoring
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - trotinette-frontend/src/shared/components/AnimatedModal.tsx
  - trotinette-frontend/src/shared/components/AnimatedDrawer.tsx
  - trotinette-frontend/src/shared/components/Navbar.tsx
  - trotinette-frontend/src/features/cart/components/CartDrawer.tsx
  - trotinette-frontend/src/features/catalog/components/ProductCard.tsx
  - trotinette-frontend/src/features/catalog/pages/CatalogPage.tsx
  - trotinette-frontend/src/app/theme.ts
autonomous: true

must_haves:
  truths:
    - "Modals open with a scale-up + blur entrance animation and close with a scale-down + blur exit"
    - "Cart drawer slides in with a smooth spring animation and blurs the backdrop"
    - "Buttons have subtle hover scale and tap feedback via Framer Motion"
    - "Navbar has a smooth appearance with enhanced active-item glow indicator"
    - "Product cards on catalog page have staggered entrance animation and enhanced hover effects"
    - "All animations respect reduced motion preference"
    - "Existing MIRAI color palette is unchanged"
  artifacts:
    - path: "trotinette-frontend/src/shared/components/AnimatedModal.tsx"
      provides: "Reusable animated modal wrapper using MUI Dialog + Framer Motion scale+blur"
      exports: ["AnimatedModal"]
      min_lines: 40
    - path: "trotinette-frontend/src/shared/components/AnimatedDrawer.tsx"
      provides: "Reusable animated drawer wrapper using MUI Drawer + Framer Motion slide+blur"
      exports: ["AnimatedDrawer"]
      min_lines: 30
    - path: "trotinette-frontend/src/shared/components/Navbar.tsx"
      provides: "Enhanced navbar with active-item glow and smooth menu animations"
    - path: "trotinette-frontend/src/features/catalog/pages/CatalogPage.tsx"
      provides: "Catalog with staggered product card entrance animations"
  key_links:
    - from: "trotinette-frontend/src/shared/components/AnimatedModal.tsx"
      to: "framer-motion AnimatePresence + modalVariants"
      via: "AnimatePresence wrapping Dialog with PaperComponent={motion.div}"
      pattern: "AnimatePresence|modalVariants"
    - from: "trotinette-frontend/src/features/cart/components/CartDrawer.tsx"
      to: "AnimatedDrawer or framer-motion"
      via: "Drawer enhanced with slide + blur animation"
      pattern: "AnimatedDrawer|motion"
---

<objective>
Create reusable AnimatedModal and AnimatedDrawer components, enhance the navbar with active-item glow, add Framer Motion hover/tap effects to buttons, animate product cards on catalog page, and enhance the cart drawer animation.

Purpose: Success criteria #4 — animated modals/drawers with scale+blur, button ripple/hover effects, sidebar active-item glow. Also covers remaining catalog animations.
Output: All interactive UI elements have smooth, consistent animations.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-RESEARCH.md
@.planning/phases/06-ui-ux-futuristic-design-refactoring/06-01-SUMMARY.md
@trotinette-frontend/src/shared/components/Navbar.tsx
@trotinette-frontend/src/features/cart/components/CartDrawer.tsx
@trotinette-frontend/src/features/catalog/components/ProductCard.tsx
@trotinette-frontend/src/features/catalog/pages/CatalogPage.tsx
@trotinette-frontend/src/shared/animations/variants.ts
@trotinette-frontend/src/app/theme.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnimatedModal and AnimatedDrawer, enhance CartDrawer</name>
  <files>
    trotinette-frontend/src/shared/components/AnimatedModal.tsx
    trotinette-frontend/src/shared/components/AnimatedDrawer.tsx
    trotinette-frontend/src/features/cart/components/CartDrawer.tsx
  </files>
  <action>
    1. Create AnimatedModal.tsx:
       - Props: { open, onClose, title?, children, maxWidth?, fullWidth? } — same interface as MUI Dialog for drop-in replacement
       - Use MUI Dialog with PaperComponent set to motion.div
       - PaperProps: variants from modalVariants (shared), initial='hidden', animate='visible', exit='exit'
       - Paper sx: glassmorphic background rgba(17,17,22,0.92), backdropFilter 'blur(20px)', border rgba(255,255,255,0.08), borderRadius 16px
       - Wrap in AnimatePresence mode="wait" keyed on open state
       - Backdrop: add backdropFilter 'blur(4px)' via slotProps.backdrop.sx
       - If title provided, render DialogTitle with cyan bottom border accent
       - NOTE: Do NOT animate backdrop-filter dynamically — set it statically per research anti-pattern

    2. Create AnimatedDrawer.tsx:
       - Props: { open, onClose, anchor?, children, width? }
       - Use MUI Drawer with PaperProps containing motion.div behavior
       - Slide in from right (or anchor side) using Framer Motion: initial={{ x: '100%' }}, animate={{ x: 0 }}, exit={{ x: '100%' }}
       - Spring transition: damping 28, stiffness 300 for snappy but smooth feel
       - Paper sx: glassmorphic background, border-left with rgba(0,194,255,0.1)
       - Backdrop: static blur(4px)

    3. Enhance CartDrawer.tsx:
       - Replace the plain MUI Drawer with the new AnimatedDrawer component (or apply equivalent animation inline if simpler)
       - Add motion.div wrappers on cart items for staggered entrance using staggerContainer + fadeInUp
       - Checkout button: whileHover={{ scale:1.02 }}, whileTap={{ scale:0.98 }}
       - Keep ALL cart logic (items, totals, handleCheckout, navigate) unchanged
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>AnimatedModal and AnimatedDrawer exist as reusable components, CartDrawer uses animated slide+blur, cart items animate in with stagger, all cart functionality preserved</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Navbar, ProductCard hover effects, and CatalogPage entrance animations</name>
  <files>
    trotinette-frontend/src/shared/components/Navbar.tsx
    trotinette-frontend/src/features/catalog/components/ProductCard.tsx
    trotinette-frontend/src/features/catalog/pages/CatalogPage.tsx
    trotinette-frontend/src/app/theme.ts
  </files>
  <action>
    1. Enhance Navbar.tsx:
       - Add active-item glow: for the currently active nav link (match via useLocation), add a subtle bottom indicator with boxShadow '0 2px 8px rgba(0,194,255,0.4)' and a small rounded cyan bar below the text
       - Dropdown menus: add motion.div wrapper with fadeInUp variant for smooth open animation (if using Menu, wrap MenuItem children in motion.div with staggered entrance)
       - Mobile drawer: if the navbar has a mobile hamburger drawer, apply slide animation similar to AnimatedDrawer
       - Keep ALL existing navbar logic (auth state, cart badge, search, admin menu, theme toggle) unchanged

    2. Enhance ProductCard.tsx:
       - Wrap the card in motion.div (or use motion(Card)) with:
         - whileHover={{ y: -8, boxShadow: '0 20px 40px rgba(0,194,255,0.12)' }} using springSnappy transition
         - whileTap={{ scale: 0.98 }} for click feedback
       - Keep existing card content, image, pricing, stock indicator, link behavior unchanged
       - The card already has CSS hover effects — replace CSS transform/boxShadow hover with Framer Motion equivalents for smoother 60fps animation. Remove the duplicate CSS hover transform to avoid conflict.

    3. Enhance CatalogPage.tsx:
       - Wrap the product grid in motion.div with staggerContainer variant
       - Each ProductCard wrapper gets fadeInUp with whileInView={{ opacity:1, y:0 }} and viewport={{ once:true, amount:0.1 }}
       - This creates a staggered waterfall entrance as user scrolls through catalog

    4. In theme.ts:
       - Enhance MuiButton component overrides to add smooth transition for hover scale: add transition: 'all 0.25s cubic-bezier(0.25,0.46,0.45,0.94)' (already partially there)
       - Verify the existing color palette (MIRAI_CYAN, MIRAI_BLACK, etc.) is UNCHANGED — this task must NOT modify any color values
       - Add MuiDialog component override for default glassmorphic styling (rgba background, blur, border) so even non-AnimatedModal dialogs look consistent
  </action>
  <verify>
    cd trotinette-frontend && npx tsc --noEmit && npm run build
  </verify>
  <done>Navbar has active-item glow and animated dropdowns, product cards animate on hover via Framer Motion, catalog page has staggered entrance, theme enhanced with animated dialog defaults, color palette unchanged, build succeeds</done>
</task>

</tasks>

<verification>
- Opening any modal shows scale+blur entrance, closing shows smooth exit
- Cart drawer slides in with spring animation, items animate in with stagger
- Navbar shows active page indicator with glow
- Product cards in catalog have smooth hover elevation via Framer Motion
- Scrolling through catalog triggers staggered card entrance
- All buttons have subtle hover/tap feedback
- Color palette in theme.ts is identical to before (MIRAI_CYAN=#00C2FF, etc.)
- All interactive elements remain keyboard navigable
- Build succeeds, no TypeScript errors
</verification>

<success_criteria>
AnimatedModal and AnimatedDrawer provide consistent scale+blur and slide+blur animations, navbar has active-item glow, buttons have hover/tap effects, catalog products animate on entrance, cart drawer is smoothly animated, existing color palette preserved, all functionality works correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ui-ux-futuristic-design-refactoring/06-05-SUMMARY.md`
</output>
