---
phase: 04-cart-checkout-orders
plan: 04
type: execute
wave: 2
depends_on: ["04-02", "04-03"]
files_modified:
  - trotinette-frontend/src/features/orders/types.ts
  - trotinette-frontend/src/features/orders/api/orders.ts
  - trotinette-frontend/src/features/orders/components/OrderStatusChip.tsx
  - trotinette-frontend/src/features/orders/pages/MyOrdersPage.tsx
  - trotinette-frontend/src/features/orders/pages/AdminOrdersPage.tsx
  - trotinette-frontend/src/features/orders/pages/AdminOrderDetailPage.tsx
  - trotinette-frontend/src/features/admin/api/deliveryZones.ts
  - trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx
  - trotinette-frontend/src/app/router.tsx
  - trotinette-frontend/src/shared/components/Navbar.tsx
  - trotinette-frontend/src/locales/fr/translation.json
  - trotinette-frontend/src/locales/en/translation.json
autonomous: true

must_haves:
  truths:
    - "Customer can view their order history with status badges"
    - "Admin can view order list filtered by status, city, and date"
    - "Admin can transition order status — only valid transition buttons are shown"
    - "Admin can add notes to orders"
    - "All status transitions in audit log are visible on admin order detail"
    - "Admin can manage delivery zones via CRUD UI"
  artifacts:
    - path: "trotinette-frontend/src/features/orders/pages/MyOrdersPage.tsx"
      provides: "Customer order history"
      contains: "MyOrdersPage"
    - path: "trotinette-frontend/src/features/orders/pages/AdminOrdersPage.tsx"
      provides: "Admin order list with filters"
      contains: "AdminOrdersPage"
    - path: "trotinette-frontend/src/features/orders/pages/AdminOrderDetailPage.tsx"
      provides: "Order detail with status transitions, notes, audit log"
      contains: "AdminOrderDetailPage"
    - path: "trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx"
      provides: "Admin delivery zone CRUD"
      contains: "AdminDeliveryZonesPage"
  key_links:
    - from: "trotinette-frontend/src/app/router.tsx"
      to: "MyOrdersPage"
      via: "ProtectedRoute children /orders"
      pattern: "customer order history route"
    - from: "trotinette-frontend/src/app/router.tsx"
      to: "AdminOrdersPage, AdminOrderDetailPage"
      via: "AdminRoute children /admin/orders"
      pattern: "admin order routes"
    - from: "AdminOrderDetailPage"
      to: "useTransitionOrder"
      via: "status transition buttons"
      pattern: "valid-only buttons from allowed_transitions"
---

<objective>
Order history and admin order management frontend: customer order history page, admin order list with filters, admin order detail with status transitions and notes, admin delivery zone CRUD, and OrderStatusChip component.

Purpose: Completes the frontend for the order lifecycle. Customers track their orders; admins manage orders through the state machine and manage delivery zones. This plan also wires the admin user detail page to show real order history (replacing the Phase 3 placeholder empty array).
Output: Orders feature folder (types, API hooks, components, pages), admin delivery zones page, route registration, translation keys.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
@C:/Users/User/.claude/get-shit-done/references/ui-brand.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cart-checkout-orders/04-RESEARCH.md
@trotinette-frontend/src/features/admin/api/products.ts
@trotinette-frontend/src/features/admin/pages/AdminProductsPage.tsx
@trotinette-frontend/src/features/admin/pages/AdminUserDetailPage.tsx
@trotinette-frontend/src/features/admin/pages/AdminCategoriesPage.tsx
@trotinette-frontend/src/app/router.tsx
@trotinette-frontend/src/locales/fr/translation.json
@trotinette-frontend/src/locales/en/translation.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Order types, API hooks, and OrderStatusChip</name>
  <files>
    trotinette-frontend/src/features/orders/types.ts
    trotinette-frontend/src/features/orders/api/orders.ts
    trotinette-frontend/src/features/orders/components/OrderStatusChip.tsx
  </files>
  <action>
    1. Create `features/orders/types.ts`:
       - OrderStatus: 'pending' | 'confirmed' | 'dispatched' | 'delivered' | 'cancelled'
       - OrderItem: id, product_id, product_sku, unit_price, quantity, subtotal, product? ({id, name})
       - OrderStatusLog: id, from_status, to_status, from_status_label, to_status_label, actor_id, actor_type, note, created_at
       - Order: id, order_number, phone, status (OrderStatus), status_label, subtotal, delivery_fee, total, note, delivery_zone ({id, city, fee}), items (OrderItem[]), status_logs? (OrderStatusLog[]), allowed_transitions (string[]), user? ({id, name, email}), created_at, updated_at
       - PaginatedOrders: data (Order[]), meta (pagination)

    2. Create `features/orders/api/orders.ts`:
       - `useMyOrders(page)`: useQuery, GET /orders?page={page}, queryKey ['orders', {page}]
       - `useAdminOrders(filters)`: useQuery, GET /admin/orders with query params (filter[status], filter[delivery_zone_id], filter[date_from], filter[date_to], page, sort). queryKey ['admin', 'orders', filters]
       - `useAdminOrder(orderId)`: useQuery, GET /admin/orders/{orderId}, queryKey ['admin', 'orders', orderId]
       - `useTransitionOrder()`: useMutation, PATCH /admin/orders/{orderId}/status with {status, note?}. onSuccess: invalidate ['admin', 'orders'] and ['admin', 'orders', orderId] and ['orders'] (customer list too)
       - `useAddOrderNote()`: useMutation, POST /admin/orders/{orderId}/note with {note}. onSuccess: invalidate ['admin', 'orders', orderId]

    3. Create `features/orders/components/OrderStatusChip.tsx`:
       - Props: status (string)
       - MUI Chip with color mapping: pending=warning, confirmed=info, dispatched=primary, delivered=success, cancelled=error
       - Label from useTranslation: t(`orders.status.${status}`)
       - size="small"
  </action>
  <verify>
    Build check: `cd trotinette-frontend && npx tsc --noEmit` — no type errors.
  </verify>
  <done>
    Order types defined. API hooks for customer orders, admin orders, status transitions, and notes. OrderStatusChip renders colored badges with translated labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Customer order history page</name>
  <files>
    trotinette-frontend/src/features/orders/pages/MyOrdersPage.tsx
  </files>
  <action>
    1. Create `features/orders/pages/MyOrdersPage.tsx`:
       - Protected route (router wraps in ProtectedRoute)
       - Page title: t('orders.myOrders')
       - Use useMyOrders(page) with pagination state
       - Display: MUI Table or Card list of orders:
         - Columns/fields: Order number, date (formatted), status (OrderStatusChip), items count, total (formatCurrency), delivery city
         - Click order row → expand or navigate to show items detail inline (no separate detail page for customers — keep it simple)
       - Empty state: "No orders yet" message with "Browse Products" link
       - Pagination: MUI TablePagination or Pagination component
       - Each order expandable/accordion showing: order items (name, qty, unit price, subtotal), delivery fee, total
  </action>
  <verify>
    Build check: `cd trotinette-frontend && npx tsc --noEmit`
  </verify>
  <done>
    Customer can view paginated order history with status badges, expandable order details showing items and totals.
  </done>
</task>

<task type="auto">
  <name>Task 3: Admin order list and detail pages</name>
  <files>
    trotinette-frontend/src/features/orders/pages/AdminOrdersPage.tsx
    trotinette-frontend/src/features/orders/pages/AdminOrderDetailPage.tsx
  </files>
  <action>
    1. Create `features/orders/pages/AdminOrdersPage.tsx`:
       - Filter bar at top with:
         - Status select (MUI Select): all statuses + "All" option
         - Delivery zone select: populated from useDeliveryZones() — import from `../../checkout/api/deliveryZones` (created in 04-03). Alternatively, if a shared location is cleaner, move the hook to `shared/api/deliveryZones.ts` and update the 04-03 import.
         - Date range: two MUI DatePicker or TextField type="date" for date_from and date_to
         - Filters stored in URL search params (same pattern as catalog FilterBar)
       - Order table (MUI Table):
         - Columns: Order #, Date, Customer (name), Status (OrderStatusChip), City, Total (formatCurrency), Items count
         - Sortable columns: created_at, total
         - Click row → navigate to /admin/orders/{id}
       - Pagination: MUI TablePagination
       - Use useAdminOrders(filters)

    2. Create `features/orders/pages/AdminOrderDetailPage.tsx`:
       - Use useAdminOrder(orderId) from route param
       - Sections:
         a. **Order Header**: Order number, date, status chip, customer info (name, email, phone — link to /admin/users/{id})
         b. **Order Items**: Table with product SKU, name, unit price, quantity, subtotal
         c. **Order Summary**: Subtotal, delivery fee, delivery city, total — all formatted MAD
         d. **Status Transitions**: Render buttons ONLY for statuses in order.allowed_transitions (from API response). Cancel button uses error color. Each button calls useTransitionOrder mutation. Buttons disabled while mutation isPending. Optional: show note textarea before transition (especially for cancellation).
         e. **Order Notes**: Display current note. "Add Note" form with textarea + submit button. Uses useAddOrderNote mutation. For recording phone confirmation outcomes per ORDR-12.
         f. **Audit Log**: Table of order.status_logs ordered by created_at desc. Columns: timestamp, from → to status, actor (name or "Customer"/"Admin"), note.
       - Loading and error states with MUI Skeleton / Alert
  </action>
  <verify>
    Build check: `cd trotinette-frontend && npx tsc --noEmit`
  </verify>
  <done>
    Admin can view filtered order list, open order detail, transition status via valid-only buttons, add notes, and view full audit log.
  </done>
</task>

<task type="auto">
  <name>Task 4: Admin delivery zones page, route registration, and translations</name>
  <files>
    trotinette-frontend/src/features/admin/api/deliveryZones.ts
    trotinette-frontend/src/features/admin/pages/AdminDeliveryZonesPage.tsx
    trotinette-frontend/src/app/router.tsx
    trotinette-frontend/src/shared/components/Navbar.tsx
    trotinette-frontend/src/locales/fr/translation.json
    trotinette-frontend/src/locales/en/translation.json
  </files>
  <action>
    1. Create `features/admin/api/deliveryZones.ts`:
       - `useAdminDeliveryZones()`: useQuery, GET /admin/delivery-zones, queryKey ['admin', 'delivery-zones']
       - `useCreateDeliveryZone()`: useMutation, POST /admin/delivery-zones. onSuccess: invalidate ['admin', 'delivery-zones'] and ['delivery-zones']
       - `useUpdateDeliveryZone()`: useMutation, PUT /admin/delivery-zones/{id}. onSuccess: invalidate both.
       - `useDeleteDeliveryZone()`: useMutation, DELETE /admin/delivery-zones/{id}. onSuccess: invalidate both.

    2. Create `features/admin/pages/AdminDeliveryZonesPage.tsx`:
       - Dialog-based CRUD (same pattern as AdminCategoriesPage — simple entity, few fields)
       - Table: city, city_ar, fee (display in MAD), is_active (Chip), actions (edit, delete)
       - Create/Edit dialog: city (required), city_ar (optional), fee in MAD input (convert to centimes for API — multiply by 100), is_active toggle
       - Delete confirmation dialog
       - Use React Hook Form + Zod for dialog forms

    3. Update `router.tsx`:
       - Add imports for: MyOrdersPage, AdminOrdersPage, AdminOrderDetailPage, AdminDeliveryZonesPage
       - Add inside ProtectedRoute children:
         - { path: '/orders', element: <MyOrdersPage /> }
       - Add inside AdminRoute children:
         - { path: '/admin/orders', element: <AdminOrdersPage /> }
         - { path: '/admin/orders/:id', element: <AdminOrderDetailPage /> }
         - { path: '/admin/delivery-zones', element: <AdminDeliveryZonesPage /> }

    4. Update `Navbar.tsx`:
       - Add "My Orders" link in the user menu (visible when logged in, navigates to /orders)
       - Add "Orders" and "Delivery Zones" links in admin navigation (alongside Products, Categories, Users)

    5. Update translation files — add order-related keys:
       - `fr/translation.json`: Add keys under "orders" namespace: myOrders, orderNumber, date, customer, status, city, total, items, noOrders, browseProducts, orderDetail, auditLog, addNote, notePlaceholder, submitNote, transitionConfirm, cancelReason. Add status sub-keys: orders.status.pending="En attente", confirmed="Confirmée", dispatched="Expédiée", delivered="Livrée", cancelled="Annulée". Add "deliveryZones" namespace: title, city, cityAr, fee, active, inactive, create, edit, delete, confirmDelete.
       - `en/translation.json`: Same keys in English. orders.status.pending="Pending", confirmed="Confirmed", dispatched="Dispatched", delivered="Delivered", cancelled="Cancelled".
  </action>
  <verify>
    1. Build check: `cd trotinette-frontend && npx tsc --noEmit` — no type errors
    2. Start dev server, verify all new routes are accessible:
       - /orders (requires login)
       - /admin/orders (requires admin)
       - /admin/orders/:id (requires admin)
       - /admin/delivery-zones (requires admin)
    3. Navbar shows cart icon, "My Orders" in user menu, "Orders" and "Delivery Zones" in admin nav
  </verify>
  <done>
    Admin delivery zone CRUD with dialog-based UI. All routes registered. Navbar updated with order and delivery zone links. FR/EN translations for orders and delivery zones complete. Phase 4 frontend fully wired.
  </done>
</task>

</tasks>

<verification>
1. Customer navigates to /orders — sees order history with status badges and expandable details
2. Admin navigates to /admin/orders — sees filtered order list (filter by status, city, date)
3. Admin clicks an order — sees detail with items, summary, status transition buttons, notes, audit log
4. Admin clicks "Confirmed" on a pending order — status updates, audit log shows new entry
5. Admin clicks "Add Note" — note appears on order
6. Only valid transition buttons shown (e.g., delivered order shows no buttons)
7. Admin navigates to /admin/delivery-zones — sees zone list, can create/edit/delete zones
8. Navbar shows "My Orders" for logged-in customers and "Orders"/"Delivery Zones" for admin
9. Language switch: all order status labels and UI text switch between FR and EN
10. No type errors on full TypeScript build
</verification>

<success_criteria>
Customer order history with status badges works. Admin order management with filters, status transitions (valid-only buttons), notes, and audit log works. Admin delivery zone CRUD works. All routes registered and navbar updated. FR/EN translations complete for orders.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cart-checkout-orders/04-04-SUMMARY.md`
</output>
