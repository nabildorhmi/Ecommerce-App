---
phase: 04-cart-checkout-orders
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - trotinette-api/app/Http/Controllers/Customer/DeliveryZoneController.php
  - trotinette-api/app/Http/Controllers/Admin/DeliveryZoneController.php
  - trotinette-api/app/Http/Requests/Admin/StoreDeliveryZoneRequest.php
  - trotinette-api/app/Http/Requests/Admin/UpdateDeliveryZoneRequest.php
  - trotinette-api/app/Http/Resources/DeliveryZoneResource.php
  - trotinette-api/routes/api.php
autonomous: true

must_haves:
  truths:
    - "GET /api/delivery-zones returns active delivery zones with id, city, and fee"
    - "Admin can create a delivery zone with city name, Arabic city name, fee, and is_active flag"
    - "Admin can update an existing delivery zone"
    - "Admin can delete a delivery zone"
    - "Delivery zone fee is stored and returned in centimes"
  artifacts:
    - path: "trotinette-api/app/Http/Controllers/Customer/DeliveryZoneController.php"
      provides: "Public delivery zones list endpoint"
      exports: ["index"]
    - path: "trotinette-api/app/Http/Controllers/Admin/DeliveryZoneController.php"
      provides: "Admin delivery zone CRUD"
      exports: ["index", "store", "show", "update", "destroy"]
    - path: "trotinette-api/app/Http/Resources/DeliveryZoneResource.php"
      provides: "Consistent JSON serialization for delivery zones"
      contains: "DeliveryZoneResource"
  key_links:
    - from: "trotinette-api/routes/api.php"
      to: "Customer\\DeliveryZoneController"
      via: "public route GET /delivery-zones"
      pattern: "DeliveryZoneController"
    - from: "trotinette-api/routes/api.php"
      to: "Admin\\DeliveryZoneController"
      via: "admin route group with role:admin middleware"
      pattern: "DeliveryZoneController"
---

<objective>
Delivery zones backend: public API endpoint for customer checkout city selection and admin CRUD for managing delivery zones.

Purpose: The checkout flow (04-03) needs GET /api/delivery-zones to display city options with fees. The admin needs CRUD to manage the city/fee list. The DeliveryZone model, migration, and seeder already exist from Phase 2 — this plan only adds controllers, requests, resource, and routes.
Output: GET /api/delivery-zones (public), full admin CRUD under /api/admin/delivery-zones, DeliveryZoneResource for consistent serialization.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cart-checkout-orders/04-RESEARCH.md
@trotinette-api/app/Models/DeliveryZone.php
@trotinette-api/database/seeders/DeliveryZoneSeeder.php
@trotinette-api/routes/api.php
@trotinette-api/app/Http/Controllers/Admin/CategoryController.php
@trotinette-api/app/Http/Resources/CategoryResource.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: DeliveryZone resource, form requests, and public endpoint</name>
  <files>
    trotinette-api/app/Http/Resources/DeliveryZoneResource.php
    trotinette-api/app/Http/Requests/Admin/StoreDeliveryZoneRequest.php
    trotinette-api/app/Http/Requests/Admin/UpdateDeliveryZoneRequest.php
    trotinette-api/app/Http/Controllers/Customer/DeliveryZoneController.php
  </files>
  <action>
    1. Create `DeliveryZoneResource.php` in `app/Http/Resources/`:
       - Return: id, city, city_ar, fee (integer centimes), is_active, created_at, updated_at
       - Follow the same pattern as CategoryResource

    2. Create `StoreDeliveryZoneRequest.php` in `app/Http/Requests/Admin/`:
       - authorize(): return true (admin middleware handles auth)
       - rules(): city (required, string, max:100, unique:delivery_zones,city), city_ar (nullable, string, max:100), fee (required, integer, min:0), is_active (sometimes, boolean)

    3. Create `UpdateDeliveryZoneRequest.php` in `app/Http/Requests/Admin/`:
       - authorize(): return true
       - rules(): city (sometimes, string, max:100, Rule::unique('delivery_zones')->ignore($this->route('delivery_zone'))), city_ar (sometimes, nullable, string, max:100), fee (sometimes, integer, min:0), is_active (sometimes, boolean)

    4. Create `Customer/DeliveryZoneController.php`:
       - `index()`: Return active delivery zones ordered by city name
       - Query: `DeliveryZone::where('is_active', true)->orderBy('city')->get()`
       - Return: `DeliveryZoneResource::collection($zones)`
       - This is the public endpoint for checkout city selection (DLVR-03)
  </action>
  <verify>
    Run `php artisan route:list --path=delivery` — should show the public GET route after Task 2 registers it.
  </verify>
  <done>
    DeliveryZoneResource serializes all zone fields. StoreDeliveryZoneRequest validates city uniqueness. Customer/DeliveryZoneController returns active zones ordered by city.
  </done>
</task>

<task type="auto">
  <name>Task 2: Admin delivery zone CRUD controller and route registration</name>
  <files>
    trotinette-api/app/Http/Controllers/Admin/DeliveryZoneController.php
    trotinette-api/routes/api.php
  </files>
  <action>
    1. Create `Admin/DeliveryZoneController.php` following the AdminCategoryController pattern:
       - `index()`: Return all delivery zones (including inactive) paginated, using DeliveryZoneResource::collection. Order by city.
       - `store(StoreDeliveryZoneRequest $request)`: Create zone, return DeliveryZoneResource with 201 status.
       - `show(DeliveryZone $deliveryZone)`: Return single zone via DeliveryZoneResource.
       - `update(UpdateDeliveryZoneRequest $request, DeliveryZone $deliveryZone)`: Update and return.
       - `destroy(DeliveryZone $deliveryZone)`: Delete the zone. If orders reference this zone (Phase 4 will add orders table), the `restrictOnDelete` FK constraint will catch it — just let the DB exception bubble up as a 500 for now. Return 204 no content on success.

    2. Register routes in `routes/api.php`:
       - Add import: `use App\Http\Controllers\Customer\DeliveryZoneController;`
       - Add import: `use App\Http\Controllers\Admin\DeliveryZoneController as AdminDeliveryZoneController;`
       - Add public route (outside auth group, alongside products/categories): `Route::get('/delivery-zones', [DeliveryZoneController::class, 'index']);`
       - Add admin routes inside the `role:admin` prefix group:
         ```
         Route::get('/delivery-zones', [AdminDeliveryZoneController::class, 'index']);
         Route::post('/delivery-zones', [AdminDeliveryZoneController::class, 'store']);
         Route::get('/delivery-zones/{delivery_zone}', [AdminDeliveryZoneController::class, 'show']);
         Route::put('/delivery-zones/{delivery_zone}', [AdminDeliveryZoneController::class, 'update']);
         Route::patch('/delivery-zones/{delivery_zone}', [AdminDeliveryZoneController::class, 'update']);
         Route::delete('/delivery-zones/{delivery_zone}', [AdminDeliveryZoneController::class, 'destroy']);
         ```

    3. Test the seeder data is accessible:
       - Run: `php artisan db:seed --class=DeliveryZoneSeeder` (if not already seeded)
       - Verify: `curl http://localhost:8000/api/delivery-zones` returns the seeded cities with fees
  </action>
  <verify>
    Start the Laravel server and test:
    1. `curl http://localhost:8000/api/delivery-zones` — returns JSON array of active delivery zones with city and fee fields
    2. `curl -X POST /api/admin/delivery-zones -H "Authorization: Bearer {admin_token}" -d '{"city":"Agadir","fee":4500}'` — returns 201 with new zone
    3. `curl -X DELETE /api/admin/delivery-zones/{id} -H "Authorization: Bearer {admin_token}"` — returns 204
    4. `php artisan route:list --path=delivery` — shows all 7 delivery zone routes
  </verify>
  <done>
    Admin can CRUD delivery zones. Public GET /api/delivery-zones returns active zones only. Routes registered in api.php. DeliveryZoneSeeder data accessible via API.
  </done>
</task>

</tasks>

<verification>
1. `php artisan route:list --path=delivery` shows 7 routes (1 public + 6 admin)
2. GET /api/delivery-zones returns active zones ordered by city with fee in centimes
3. POST /api/admin/delivery-zones creates a new zone (requires admin token)
4. PUT /api/admin/delivery-zones/{id} updates a zone
5. DELETE /api/admin/delivery-zones/{id} removes a zone
6. Unauthenticated request to admin delivery zone routes returns 401
</verification>

<success_criteria>
Public delivery zones endpoint returns seeded Moroccan cities with fees. Admin CRUD works for all operations. All routes are protected by appropriate middleware. DeliveryZoneResource provides consistent JSON serialization.
</success_criteria>

<output>
After completion, create `.planning/phases/04-cart-checkout-orders/04-01-SUMMARY.md`
</output>
